const Styles = {
  questionPaper : {
    height: 500,
    width: 700,
    margin: 20,
    textAlign: 'center',
    display: 'inline-block',
  }
};

export default Styles;

// Question: why doesn't it work when export const Styles = {..}